extends ~@/layout

block title
  title

block content

  - const data = require('./data.js')

  #order-list-pg

    +breadcrumbs({ items: ['Каталог', 'Мебель', 'Комплекты'] })

    +page-title({ title: 'Список заказов', type: 'line' }, 'order-list-pg')

    +table-orders('order-list-pg')

